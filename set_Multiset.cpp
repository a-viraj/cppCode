#include<bits/stdc++.h>
using namespace std;
int main()
{
    //set
    /*
    set<int>s;
    s.insert(1);
    s.insert(5);
    s.insert(3);
    s.insert(4);
    for(auto i:s)
    {
        cout<<i<<' ';
    }
    cout<<endl;
    s.erase(5);
    for(auto i:s)
    {
        cout<<i<<' ';
    }
    */
   //multiset: same as set but can contain duplicates
   /*
   multiset<int>s;
   s.insert(1);
    s.insert(3);
    s.insert(3);
    s.insert(4);
    for(auto i:s)
    {
        cout<<i<<' ';
    }
    cout<<endl;
    s.erase(s.find(3));
    for(auto i:s)
    {
        cout<<i<<' ';
    }
    */
    //unordered set: implimented using hashing
    //header file: #include<unordered_set>
    
}